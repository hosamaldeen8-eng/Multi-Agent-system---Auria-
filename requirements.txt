# Auria multi-agent fleet
#
# Core: the Claude Agent SDK gives us the orchestrator loop, subagents,
# built-in tools, in-process MCP servers, and sessions.
claude-agent-sdk>=0.1.0

# Slack bridge (Socket Mode — no public URL needed for monitoring/commands)
slack-bolt>=1.20.0
slack-sdk>=3.31.0

# Config loading
python-dotenv>=1.0.0

# NOTE: the Odoo tools use xmlrpc.client from the Python standard library —
# no extra dependency required.
